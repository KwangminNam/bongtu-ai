# SST 배포용 환경변수 (루트)
# 실제 사용 시 .env 파일로 복사 후 값 입력

# Supabase PostgreSQL
DATABASE_URL="postgresql://..."

# JWT Secret (client NEXTAUTH_SECRET과 동일하게)
JWT_SECRET="your-jwt-secret"

# Google Gemini API
GOOGLE_GENERATIVE_AI_API_KEY="your-gemini-api-key"

# CORS (배포된 클라이언트 도메인)
CORS_ORIGIN="https://your-app.vercel.app"
